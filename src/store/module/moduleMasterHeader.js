import state from '../state/stateMasterHeader';
import actions from '../action/actionMasterHeader';
import mutations from '../motation/motationMasterHeader';

export default {
    // namespaced: true,
    state,
    actions,
    mutations,
    gettters: {}
}