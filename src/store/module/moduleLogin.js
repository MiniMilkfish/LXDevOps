import state from '../state/stateLogin';
import actions from '../action/actionLogin';
import mutations from '../motation/motationLogin';

export default {
    // namespaced: true,
    state,
    actions,
    mutations,
    gettters: {}
}